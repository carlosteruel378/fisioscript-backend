{
  "name": "fisioscript-backend",
  "version": "2.0.0",
  "type": "module",
  "description": "FisioScript API — Documentación clínica con IA",
  "scripts": {
    "start": "node --import ./instrument.js server.js",
    "dev": "node --import ./instrument.js --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@sentry/node": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^14.21.0"
  }
}
